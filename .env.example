# Copy to .env (gitignored) and fill in. GitHub push protection rejects Mapbox
# tokens in commits, so the token only ever lives here and in Vercel env vars.
# scripts/write_mapbox_token.py turns this into frontend/assets/mapbox-token.js.
MAPBOX_TOKEN=pk.your-mapbox-public-token-here
